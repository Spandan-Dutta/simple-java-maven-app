pipeline {
  agent any

  tools {
    tool name: 'M399', type: 'maven' 
  }

  stages {

    stage('Build') {
      steps {
        sh 'mvn clean package'
      }
      post {
        success {
          archiveArtifacts artifacts: '**/target/*.war'
        }
      }
    }

    
  }

}